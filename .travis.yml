language: java

jdk:
  - oraclejdk8

os:
  - linux

branches:
  only:
    - master

cache:
  directories:
  - $HOME/.gradle

before_script:
  - git fetch
  - git checkout -b gh-pages
  - git merge origin/master

script:
  - cd app
  - ./gradlew build
